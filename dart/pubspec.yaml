name: unit_conversion
description: A robust unit conversion library for Dart, covering speed, mass, distance, etc.
version: 1.0.0
repository: https://github.com/hidenorly/unit_conversion

environment:
  sdk: ^3.6.0

# Add regular dependencies here.
dependencies:

dev_dependencies:
  test: ^1.25.15

